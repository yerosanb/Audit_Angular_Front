import { MaxFailedAndJwtControl } from './max-failed-and-jwt-control';

describe('MaxFailedAndJwtControl', () => {
  it('should create an instance', () => {
    expect(new MaxFailedAndJwtControl()).toBeTruthy();
  });
});
