import { OnlineFailedUsers } from './online-failed-users';

describe('OnlineFailedUsers', () => {
  it('should create an instance', () => {
    expect(new OnlineFailedUsers()).toBeTruthy();
  });
});
