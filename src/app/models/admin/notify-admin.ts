export class NotifyAdmin {
id ?: number;
user_name ?: string;
failed? : boolean;
expired ?: boolean;
locked ?: boolean;
failed_status ? : number;
expired_status ? :number;
locked_status ?: number;
loan_notification_admin?:Number;
}

