import { JobPositionsByRole } from './job-positions-by-role';

describe('JobPositionsByRole', () => {
  it('should create an instance', () => {
    expect(new JobPositionsByRole()).toBeTruthy();
  });
});
