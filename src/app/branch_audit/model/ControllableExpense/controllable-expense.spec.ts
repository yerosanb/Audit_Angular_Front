import { ControllableExpense } from './controllable-expense';

describe('ControllableExpense', () => {
  it('should create an instance', () => {
    expect(new ControllableExpense()).toBeTruthy();
  });
});
