import { ControllableExpenseType } from './controllable-expense-type';

describe('ControllableExpenseType', () => {
  it('should create an instance', () => {
    expect(new ControllableExpenseType()).toBeTruthy();
  });
});
