import { BranchManagerFile } from './branch-manager-file';

describe('BranchManagerFile', () => {
  it('should create an instance', () => {
    expect(new BranchManagerFile()).toBeTruthy();
  });
});
