import { CompiledRegionalAudit } from './compiled-regional-audit';

describe('CompiledRegionalAudit', () => {
  it('should create an instance', () => {
    expect(new CompiledRegionalAudit()).toBeTruthy();
  });
});
