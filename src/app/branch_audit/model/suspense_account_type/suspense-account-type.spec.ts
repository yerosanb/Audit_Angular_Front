import { SuspenseAccountType } from './suspense-account-type';

describe('SuspenseAccountType', () => {
  it('should create an instance', () => {
    expect(new SuspenseAccountType()).toBeTruthy();
  });
});
