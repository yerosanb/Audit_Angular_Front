import { NegotiableInstrument } from './negotiable-instrument';

describe('NegotiableInstrument', () => {
  it('should create an instance', () => {
    expect(new NegotiableInstrument()).toBeTruthy();
  });
});
